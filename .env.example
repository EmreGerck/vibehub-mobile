# Default: production API. For local backend dev, use http://YOUR_LAN_IP:3001
# (Android Expo Go cannot reach 'localhost' — it points to the phone itself)
EXPO_PUBLIC_API_URL=https://api.vibehub.com.tr
